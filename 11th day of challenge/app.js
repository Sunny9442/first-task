///Question 31: No Users: Ensure your user list isn’t empty.
var names = [];
console.log(names[3]);
console.log(names[0]);
if (names.length === 0) {
    console.log("we need to find some users");
}
